import { useEffect, useState } from 'react';
import type { Project } from '../../../packages/core/src/index.ts';
import { api } from './api';
export function RecorderPanel({ project, perform, busy }: { project: Project; perform: (job: () => Promise<void>) => void; busy: boolean }) {
  const [recording, setRecording] = useState(''), [route, setRoute] = useState('/'), [draft, setDraft] = useState(''), [warnings, setWarnings] = useState<string[]>([]);
  useEffect(() => { void api<{ id: string; projectId: string }[]>('/recordings').then(sessions => setRecording(sessions.find(session => session.projectId === project.id)?.id || '')).catch(() => {}); }, [project.id]);
  return <section className="panel content-panel"><h2>Record a browser flow</h2><p className="muted-text">A browser opens for your clicks, labeled inputs and checkboxes. Review the recorded steps before saving. Typed values become environment references.</p><div className="form-columns"><label>Starting route<input value={route} onChange={e => setRoute(e.target.value)} disabled={!!recording}/></label><div className="button-row">{!recording ? <button className="btn" disabled={busy} onClick={() => perform(async () => { const result = await api<{ id: string }>(`/projects/${project.id}/recordings`, 'POST', { route }); setRecording(result.id); setDraft(''); })}>Start recording</button> : <button className="btn primary" disabled={busy} onClick={() => perform(async () => { const result = await api<any>(`/recordings/${recording}/stop`, 'POST'); setRecording(''); setDraft(JSON.stringify(result.flow, null, 2)); setWarnings(result.warnings); })}>Stop and review</button>}</div></div>{recording && <p role="status">Recording in the browser window. Return here when finished.</p>}{draft && <form className="form" onSubmit={e => { e.preventDefault(); perform(async () => { await api(`/projects/${project.id}/flows`, 'POST', JSON.parse(draft)); setDraft(''); }); }}><ul>{warnings.map(warning => <li key={warning}>{warning}</li>)}</ul><label>Review flow JSON<textarea className="code-editor" rows={14} value={draft} onChange={e => setDraft(e.target.value)}/></label><button className="btn primary" disabled={busy}>Save reviewed flow</button></form>}</section>;
}
